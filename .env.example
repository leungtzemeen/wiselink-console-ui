# 开发推荐：同源 /api，电脑与手机访问 http://<局域网IP>:5173 时都由 Vite 代理到本机 Java（勿用手机上的 localhost）
VITE_API_BASE=/api

# 若仍写绝对地址：不要写「另一端口」的局域网 URL（如 http://192.168.x.x:8081），手机会直连 8081，易被防火墙/CORS 拦住。
# 应让浏览器始终请求当前页的 /api/...，由 Vite 再转到本机 127.0.0.1:8081（见 vite.config.ts proxy）。

# 仅在「浏览器页面本身就是 localhost」时用直连；手机扫局域网 IP 时不要配 localhost
# VITE_API_BASE=http://localhost:8081/api

# 生产部署（Nginx 同域静态站 + /api 反代到 Java）示例：
# VITE_API_BASE=https://wiselink.wiki/api
